import React from 'react';
import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import GlassButton from '../components/ui/GlassButton';
import { useLocalGameStore } from '../store/localGameStore';
import { gameColors } from '../theme/gameTheme';

export default function StatisticsScreen({ navigation }: any) { const stats = useLocalGameStore((state) => state.statistics); const winRate = stats.gamesPlayed ? `${Math.round((stats.wins / stats.gamesPlayed) * 100)}%` : '0%'; const items = [['GAMES PLAYED', stats.gamesPlayed], ['WINS', stats.wins], ['WIN RATE', winRate], ['BEST SCORE', stats.highestScore], ['CURRENT STREAK', stats.currentStreak], ['BEST STREAK', stats.bestStreak], ['ROUNDS WON', stats.roundsWon], ['CARDS PLAYED', stats.cardsPlayed]]; return <SafeAreaView style={styles.safe}><View style={styles.content}><Text style={styles.eyebrow}>YOUR RECORD</Text><Text style={styles.title}>Statistics</Text><View style={styles.grid}>{items.map(([label, value]) => <View style={styles.stat} key={String(label)}><Text style={styles.value}>{value}</Text><Text style={styles.label}>{label}</Text></View>)}</View><GlassButton title="BACK TO HOME" onPress={() => navigation.goBack()} /></View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: gameColors.ink }, content: { padding: 24, paddingTop: 34 }, eyebrow: { color: gameColors.aqua, fontSize: 11, letterSpacing: 2, fontWeight: '800' }, title: { color: gameColors.cloud, fontSize: 36, fontWeight: '900', marginTop: 8, marginBottom: 28 }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: 10, marginBottom: 28 }, stat: { width: '47%', backgroundColor: gameColors.navy, borderRadius: 12, padding: 16, minHeight: 90 }, value: { color: gameColors.gold, fontSize: 28, fontWeight: '900' }, label: { color: gameColors.muted, fontSize: 10, fontWeight: '800', letterSpacing: 1, marginTop: 8 } });