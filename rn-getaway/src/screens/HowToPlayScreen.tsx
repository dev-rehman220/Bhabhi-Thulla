import React from 'react';
import { SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import GlassButton from '../components/ui/GlassButton';
import { gameColors } from '../theme/gameTheme';

export default function HowToPlayScreen({ navigation }: any) { const rules = [['01', 'DRAW', 'Take a card when nothing in your hand matches the top card.'], ['02', 'MATCH', 'Play a card with the same suit or rank as the discard.'], ['03', 'COLLECT', 'Each card you play adds its value to your score. Clear your hand to win.'], ['04', 'OUTPLAY', 'Your opponent plays the lowest valid card. Build your score before they do.']]; return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.eyebrow}>CLASSIC MODE</Text><Text style={styles.title}>Find your way out.</Text><Text style={styles.intro}>A clean hand beats a clever hand. Match the table, make the call, and be first to play every card.</Text>{rules.map(([number, title, body]) => <View style={styles.rule} key={number}><Text style={styles.number}>{number}</Text><View><Text style={styles.ruleTitle}>{title}</Text><Text style={styles.body}>{body}</Text></View></View>)}<View style={styles.tip}><Text style={styles.tipTitle}>SCORING</Text><Text style={styles.body}>Number cards score their face value. J, Q, K and A are worth 11, 12, 13 and 14. Higher scores win the round.</Text></View><GlassButton title="BACK TO HOME" onPress={() => navigation.goBack()} /></ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: gameColors.ink }, content: { padding: 24, paddingTop: 34 }, eyebrow: { color: gameColors.aqua, fontSize: 11, letterSpacing: 2, fontWeight: '800' }, title: { color: gameColors.cloud, fontSize: 34, fontWeight: '900', marginTop: 10 }, intro: { color: gameColors.muted, lineHeight: 23, fontSize: 16, marginTop: 12, marginBottom: 28 }, rule: { flexDirection: 'row', gap: 18, paddingVertical: 17, borderTopWidth: 1, borderTopColor: gameColors.line }, number: { color: gameColors.gold, fontWeight: '900', fontSize: 16 }, ruleTitle: { color: gameColors.cloud, fontWeight: '900', letterSpacing: 1, marginBottom: 5 }, body: { color: gameColors.muted, lineHeight: 20, maxWidth: 290 }, tip: { backgroundColor: 'rgba(111,224,208,0.1)', borderRadius: 12, padding: 18, marginVertical: 26 }, tipTitle: { color: gameColors.aqua, fontWeight: '900', letterSpacing: 1, marginBottom: 7 } });